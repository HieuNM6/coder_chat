<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<% if logged_in? %>
  <h2>
    wellcome <%= current_user.name  %>! | <%= link_to "Logout", logout_path, 
      method: :delete %>
  </h2>
<% else %>
  <%= link_to "Register", new_user_path %> | <%= link_to "Login", login_path %>
<% end %>
